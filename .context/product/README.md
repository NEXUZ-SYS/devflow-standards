# product/ — reservado

Camada de **produto** do layout DDC. Reservada para conteúdo default futuro
(ex.: princípios de produto/design que o DevFlow possa shippar por padrão).

Hoje vazia de conteúdo fetchável. O `/devflow update` busca **apenas** os
`std-*.md` de `engineering/standards/` — esta pasta não é consumida pelo fetch.
